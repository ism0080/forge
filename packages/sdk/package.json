{
  "name": "@forge/sdk",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "publish:local": "npm version prerelease && npm publish --registry http://localhost:4873"
  },
  "dependencies": {
    "@forge/core": "workspace:*"
  }
}
