{
  "name": "@ism0080/forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev:api": "pnpm --filter @ism0080/forge-server dev",
    "dev:cli": "pnpm --filter @ism0080/forge-cli dev",
    "docker": "docker compose up -d",
    "docker:new": "docker compose up --force-recreate --build -d",
    "typecheck": "pnpm -r typecheck",
    "cli:global": "pnpm --filter @ism0080/forge-cli build && pnpm add -g ./packages/cli",
    "cli:link": "pnpm --filter @ism0080/forge-cli build && pnpm --dir packages/cli link --global",
    "lint": "oxlint",
    "format": "oxfmt",
    "publish:all": "pnpm -r build && pnpm -r exec npm version patch --no-git-tag-version && pnpm -r publish --access public --no-git-checks"
  },
  "dependencies": {
    "oxfmt": "^0.54.0",
    "oxlint": "^1.69.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.12.1"
}
